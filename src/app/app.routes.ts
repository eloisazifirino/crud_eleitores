import { Routes } from '@angular/router';

// A lista está vazia porque a aplicação possui uma única tela.
// Novas páginas podem ser associadas a caminhos adicionando objetos neste array.
export const routes: Routes = [];
